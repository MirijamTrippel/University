import java.util.ArrayList;
import java.util.Iterator;
/**
 * Beschreiben Sie hier die Klasse test.
 * 
 * @author (Ihr Name) 
 * @version (eine Versionsnummer oder ein Datum)
 */
public class test
{
    private ArrayList<String> list;
    
    /**
     * Konstruktor für Objekte der Klasse test
     */
    public void createArrayList()
    {
        list = new ArrayList<>();
    }

    /**
     * Ein Beispiel einer Methode - ersetzen Sie diesen Kommentar mit Ihrem eigenen
     * 
     * @param  y    ein Beispielparameter für eine Methode
     * @return        die Summe aus x und y
     */
    public void beispielMethode(int y)
    {
        for (String aString: list){
            //do something // goes once through every elemt in the whole list
            
        }
        
        String word = new String ("a");
        // create a new object 
        String Otherword = new String ("b");
       
        // return word.equals(otherword);
        
        // compare both objects(strings) with the equal method
        // they are equal but not identical
        // if you compare obects every object is unique, even if they have the same values.
    }
   
   
    
    
    
    
    
}
